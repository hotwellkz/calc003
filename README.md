# Калькулятор стоимости строительства HotWell.kz

Отдельное standalone приложение калькулятора стоимости строительства СИП домов, готовое для встраивания через iframe.

## 📋 Описание

Это приложение было вырезано из основного проекта и адаптировано для работы в "гостевом" режиме без авторизации. Калькулятор позволяет рассчитать стоимость строительства СИП дома в черновую отделку с учетом всех параметров.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр production сборки

```bash
npm run preview
```

## 📁 Структура проекта

```
calculator-standalone/
├── src/
│   ├── components/
│   │   └── calculator/
│   │       ├── CalculatorForm.tsx      # Форма ввода параметров
│   │       ├── PriceSummary.tsx        # Итоговая стоимость
│   │       ├── CommercialProposal.tsx  # Коммерческое предложение
│   │       ├── Input.tsx               # Компонент ввода
│   │       └── Dropdown.tsx            # Компонент выпадающего списка
│   ├── pages/
│   │   └── Calculator.tsx              # Главная страница калькулятора
│   ├── types/
│   │   └── calculator.ts               # TypeScript типы
│   ├── utils/
│   │   ├── calculatorData.ts           # Статические данные калькулятора
│   │   ├── calculatePrice.ts           # Логика расчета стоимости
│   │   ├── configLoader.ts             # Загрузчик конфигурации (без Firebase)
│   │   └── deliveryData.ts             # Данные о доставке
│   ├── App.tsx                         # Корневой компонент
│   ├── main.tsx                        # Точка входа
│   └── index.css                       # Глобальные стили
├── index.html
├── package.json
├── vite.config.ts
└── README.md
```

## 🔧 Что было изменено

### Убрано из оригинального проекта:

1. **Авторизация и проверки доступа**
   - Удалены все проверки `useAuth`, `AuthGuard`, `ApprovalGuard`
   - Убрана зависимость от Firebase Authentication

2. **Firebase**
   - Убрана зависимость от Firebase Firestore для загрузки конфигурации
   - Используются только статические данные из `calculatorData.ts`
   - Админ-панель (`CalculatorAdminPanel`) полностью удалена

3. **Роутинг и навигация**
   - Убран React Router
   - Убраны все ссылки на другие страницы
   - Приложение состоит из одной страницы `/`

4. **Контексты и зависимости**
   - Убран `MenuVisibilityContext` (использовался только в CommercialProposal)
   - Упрощена версия `CommercialProposal` без управления видимостью меню

### Оставлено:

1. **Все компоненты калькулятора:**
   - `CalculatorForm` - форма ввода параметров
   - `PriceSummary` - итоговая стоимость
   - `CommercialProposal` - коммерческое предложение с экспортом в PDF
   - `Input` и `Dropdown` - UI компоненты

2. **Вся логика расчета:**
   - Функция `calculatePrice` работает полностью на клиенте
   - Все данные хранятся статически в `calculatorData.ts`

3. **Стили:**
   - Используется Tailwind CSS
   - Сохранен оригинальный дизайн

## 🌐 Настройка для встраивания через iframe

### Настройка заголовков

В `vite.config.ts` уже настроены заголовки для разрешения встраивания:

```typescript
server: {
  headers: {
    'X-Frame-Options': 'ALLOWALL',
    'Content-Security-Policy': "frame-ancestors 'self' https://hotwell.kz https://*.hotwell.kz;",
  },
}
```

### Пример встраивания на WordPress

```html
<div style="width:100%;max-width:1100px;margin:0 auto;">
  <iframe
    src="https://calculator.hotwell.kz"
    style="width:100%;height:900px;border:none;"
    loading="lazy"
    title="Калькулятор стоимости строительства"
  ></iframe>
</div>
```

**Параметры:**
- `height` - можно менять в зависимости от контента (рекомендуется 900-1200px)
- `src` - URL, где развернуто приложение
- `loading="lazy"` - ленивая загрузка для оптимизации

### Деплой

#### Netlify (рекомендуется)

1. **Подключите репозиторий GitHub:**
   - Зайдите в [Netlify Dashboard](https://app.netlify.com)
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите репозиторий `hotwellkz/calc001`

2. **Настройки сборки (должны определиться автоматически из `netlify.toml`):**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Node version: 18.x или выше (можно указать в `.nvmrc` или настройках Netlify)

3. **Важно:** 
   - Убедитесь, что в настройках сайта включена опция "Deploy settings" → "Build settings" используют `netlify.toml`
   - Файл `netlify.toml` уже настроен с правильными MIME типами для JavaScript модулей
   - Если после деплоя видите белый экран, проверьте консоль браузера - возможно нужно пересобрать сайт

4. После деплоя сайт будет доступен по адресу типа `https://your-site.netlify.app`

5. **Настройка кастомного домена:**
   - В настройках сайта → Domain settings
   - Добавьте свой домен (например, `calculator.hotwell.kz`)
   - Настройте DNS записи согласно инструкциям Netlify

#### Другие хостинги

После сборки (`npm run build`) файлы из папки `dist/` нужно загрузить на хостинг:

1. **Vercel:**
   - Подключите репозиторий
   - Build command: `npm run build`
   - Output directory: `dist`

2. **GitHub Pages:**
   - Используйте GitHub Actions для автоматической сборки
   - Или загрузите содержимое `dist/` в ветку `gh-pages`

3. **Nginx конфигурация:**
```nginx
server {
    listen 80;
    server_name calculator.hotwell.kz;
    root /var/www/calculator/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Разрешаем встраивание в iframe
    add_header X-Frame-Options "ALLOWALL" always;
    add_header Content-Security-Policy "frame-ancestors 'self' https://hotwell.kz https://*.hotwell.kz;" always;
}
```

## 📦 Зависимости

### Основные:
- `react` ^18.3.1
- `react-dom` ^18.3.1
- `lucide-react` ^0.344.0 - иконки
- `html2pdf.js` ^0.10.3 - экспорт в PDF

### Dev зависимости:
- `vite` ^5.4.2 - сборщик
- `typescript` ^5.5.3
- `tailwindcss` ^3.4.1 - стили
- `@vitejs/plugin-react` ^4.3.1

## 🔄 Работа с бэкендом

**Текущая версия работает полностью на клиенте без бэкенда.**

Все расчеты выполняются локально в браузере. Калькулятор не делает никаких API запросов - все данные статические и хранятся в коде.

### Если в будущем потребуется интеграция с API:

1. Создайте файл `src/config/api.ts`:
```typescript
export const API_CONFIG = {
  BASE_URL: import.meta.env.VITE_API_BASE_URL || 'https://api.hotwell.kz',
};
```

2. Добавьте в `.env`:
```
VITE_API_BASE_URL=https://api.hotwell.kz
```

3. Используйте в компонентах для загрузки данных (например, для обновления цен или конфигурации)

**Примечание:** В оригинальном проекте калькулятор загружал конфигурацию из Firebase Firestore через `configLoader.ts`. В standalone версии эта функциональность убрана, и используются только статические данные из `calculatorData.ts`.

## 📝 Перенесенные файлы

### Компоненты:
- ✅ `src/components/calculator/CalculatorForm.tsx` - упрощенная версия без Firebase
- ✅ `src/components/calculator/PriceSummary.tsx` - без изменений
- ✅ `src/components/calculator/CommercialProposal.tsx` - упрощенная версия без MenuVisibilityContext (убрана логика скрытия меню)
- ✅ `src/components/calculator/Input.tsx` - без изменений
- ✅ `src/components/calculator/Dropdown.tsx` - без изменений

### Утилиты:
- ✅ `src/utils/calculatorData.ts` - статические данные (все опции, цены, коэффициенты)
- ✅ `src/utils/calculatePrice.ts` - логика расчета (полностью на клиенте)
- ✅ `src/utils/configLoader.ts` - упрощенная версия без Firebase (использует только статические данные)
- ✅ `src/utils/deliveryData.ts` - данные о доставке (города и цены)

### Типы:
- ✅ `src/types/calculator.ts` - TypeScript интерфейсы (CalculatorInput, CalculationResult, CalculatorState и др.)

### Страницы:
- ✅ `src/pages/Calculator.tsx` - упрощенная версия без админ-панели и кнопки "Назад"

## 📊 Зависимости проекта

### Основные зависимости (production):
- `react` + `react-dom` - React фреймворк
- `lucide-react` - иконки (используется в компонентах)
- `html2pdf.js` - экспорт коммерческого предложения в PDF

### Dev зависимости:
- `vite` - сборщик и dev-сервер
- `typescript` - типизация
- `tailwindcss` - стили (все стили через Tailwind классы)
- `@vitejs/plugin-react` - плагин для React

### Удаленные зависимости (не нужны):
- ❌ `firebase` - не используется (конфигурация статическая)
- ❌ `react-router-dom` - не используется (одна страница)
- ❌ Все зависимости для авторизации, админ-панели и других модулей

## 🎨 Стили

Используется Tailwind CSS. Все стили инлайн в компонентах. Глобальные стили в `src/index.css`.

## 📱 Адаптивность

Приложение полностью адаптивно и корректно работает на:
- Десктопах (1920px+)
- Планшетах (768px - 1024px)
- Мобильных устройствах (< 768px)

## ⚙️ Режимы работы

Калькулятор поддерживает два режима:

1. **Обычный режим** - основные параметры для быстрого расчета
2. **Профессиональный режим** - все параметры для детальной настройки

Режим сохраняется в `localStorage` и восстанавливается при следующем посещении.

## 📄 Лицензия

Проект создан для HotWell.kz

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что все зависимости установлены (`npm install`)
3. Проверьте версию Node.js (рекомендуется 18+)
4. Убедитесь, что порт 5173 свободен (для dev-сервера)

## 📋 Краткое описание изменений

### Что было сделано:

1. ✅ Создан новый проект на Vite + React + TypeScript
2. ✅ Перенесены все компоненты калькулятора
3. ✅ Убраны зависимости от Firebase (используются только статические данные)
4. ✅ Убрана авторизация и проверки доступа
5. ✅ Убрана админ-панель калькулятора
6. ✅ Упрощен CommercialProposal (убран MenuVisibilityContext)
7. ✅ Настроена сборка для production
8. ✅ Настроены заголовки для встраивания в iframe
9. ✅ Создан подробный README с инструкциями

### Структура проекта:

```
calculator-standalone/
├── src/
│   ├── components/calculator/    # Все компоненты калькулятора
│   ├── pages/                    # Страница Calculator
│   ├── types/                    # TypeScript типы
│   ├── utils/                    # Утилиты и логика расчета
│   ├── App.tsx                   # Корневой компонент
│   └── main.tsx                  # Точка входа
├── public/                       # Статические файлы
├── package.json                  # Зависимости (минимальный набор)
├── vite.config.ts               # Конфигурация Vite с настройками iframe
└── README.md                     # Документация
```

### Команды:

- `npm install` - установка зависимостей
- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для production
- `npm run preview` - предпросмотр production сборки

### Готово к использованию:

Проект полностью готов к использованию. После установки зависимостей (`npm install`) можно сразу запускать `npm run dev` и тестировать калькулятор.

